@model  AndersonPay.Models.Manpower.Manpower

@{
    ViewBag.Title = "CreateManpower";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<!--Use This To prevent the back option of the browser...copied by Lars hekhek!-->
<script type="text/javascript">
    function preventBack() { window.history.forward(); }
    setTimeout("preventBack()", 0);
    window.onunload = function () { null };
</script>

@using (Html.BeginForm("CreateManpower", "AndersonPay", null, FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <div class="page-header">
        <div class="page-header-content">
            <div class="page-title">
                <h4><i class="icon-folder-plus position-left"></i> <span class="text-semibold">Anderson.Pay</span> - Add new file</h4>
            </div>
        </div>
        <div class="content">

            <div class="panel panel-flat">
                <div class="panel-heading">
                    <h5 class="panel-title">File Details</h5>
                    <div class="heading-elements">
                        <ul class="icons-list">
                            <li><a data-action="collapse"></a></li>
                            <li><a data-action="reload"></a></li>
                        </ul>
                    </div>
                </div>
                <div class="panel-body">
                    <div class="editor-label" style="margin-top:20px;font-family:Arial,Verdana;font-size:18px">
                        @using (Html.BeginForm("CreateManpower", "AndersonPay", null, FormMethod.Post, new { enctype = "multipart/form-data" }))
                        {
                            <div class="panel-body">

                                <div class="editor-label" style="font-family:Arial,Verdana;font-size:15px">
                                    @Html.LabelFor(model => model.Name)
                                </div>
                                <div class="editor-field">
                                    @Html.EditorFor(model => model.Name, new { htmlAttributes = new { @class = "form-control" }, })
                                    <i style="color:red;font-size:13px">@Html.ValidationMessageFor(model => model.Name)</i>
                                    <br />
                                </div>
                                <div class="editor-label" style="margin-top:20px;font-family:Arial,Verdana;font-size:15px">
                                    @Html.LabelFor(model => model.Detail)
                                </div>

                                <div class="editor-field">
                                    @Html.TextAreaFor(model => model.Detail, new { @class = "form-control", @rows = 1 })
                                    <i style="color:red;font-size:13px">@Html.ValidationMessageFor(model => model.Detail)</i>
                                </div>
                                <div class="editor-label" style="margin-top:20px;font-family:Arial,Verdana;font-size:15px">
                                    <label>Files:</label>
                                </div>
                                <div class="editor-field">
                                    <input type="file" name="file" multiple="multiple" />
                                </div>
                                <div class="text-right">
                                    <button type="submit" value="Create" class="btn btn-primary">Add To Files<i class="icon-arrow-right14 position-right"></i></button>
                                </div>
                            </div>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>


}

